# MongoFlow reproducibility environment file
# Copy this file to the project root if needed and adapt it to your local setup.

# Local MongoDB instance used by the reproducibility workflow
MONGODB_URI=mongodb://127.0.0.1:27017

# Optional AI assistant configuration
# Required only for AI-assisted query generation and error correction.
GEMINI_API_KEY=

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000